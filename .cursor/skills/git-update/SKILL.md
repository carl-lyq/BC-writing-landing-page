---
name: git-update
description: Inspects git status and diffs, summarizes changes for user review, and only commits and pushes after explicit user confirmation. Use when the user runs /git-update, asks to commit and push, summarize git changes before commit, or sync work to remote after reviewing diffs.
disable-model-invocation: true
---

# Git Update

本仓库使用个人技能 **`~/.cursor/skills/git-update/`**（跨项目通用）。

运行 `/git-update` 或 `@git-update` 即可；Agent 会加载个人目录下的完整流程。

本地副本仅作索引；**以 `~/.cursor/skills/git-update/SKILL.md` 为准**。
